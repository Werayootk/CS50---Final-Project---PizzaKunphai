@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered Foods of the week </h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var food in Model.PreferredFoods)
    {
        @Html.Partial("FoodsSummary", food)
    }
</div>